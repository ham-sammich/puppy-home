[package]
name = "puppy-relay"
version = "0.1.0"
edition = "2024"
description = "Tiny relay server for Puppy Pack rooms: presence + chat + activity."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
